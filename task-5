def factor(a):
    for i in range(1,a+1):
        if a%i==0:
            print(i)

input_num=int(input('Enter the number : '))

if input_num <=0:
    print('Invalid Number !!')
    
else:
    factor(input_num)
